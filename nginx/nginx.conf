events {
    worker_connections 1024;
}

http {
    upstream backend {
        server spring-app1:8080;
        server spring-app2:8080;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}
